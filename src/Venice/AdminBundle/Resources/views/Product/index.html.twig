{% extends('VeniceAdminBundle::Product/base.html.twig') %}

{% block pageTitle %}
    Products
{% endblock %}

{% block pageBody %}
    <div class="box">
        {#todo: tempoary... waitong for grid#}
        <ul>
            {% for product in products %}
                <li>
                    <a href="{{ path("admin_product_tabs", {'id':product.id}) }}">{{ product.name }}</a>
                </li>
            {% endfor %}
        </ul>

        <div id="product-grid"  data-config="{{ gridConfiguration }}">
        </div>
    </div>
{% endblock %}